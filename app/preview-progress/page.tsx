import type { Metadata } from "next";
import SitePreview from "../site-preview";

export const metadata: Metadata = {
  title: "Auctrail Development Preview",
  robots: { index: false, follow: false },
};

export default SitePreview;
